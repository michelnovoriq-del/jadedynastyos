import { LoadingShell } from "@/components/demo/loading-shell";

export default function Loading() {
  return <LoadingShell title="Loading workspace" subtitle="Preparing the booking system view." />;
}

